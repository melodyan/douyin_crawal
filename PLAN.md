# 多平台视频采集、下载、转写与报告工具实施计划

## 1. 目标与已确认的需求

- 当前只实现抖音；配置中的 `platform` 默认是 `douyin`，并预留 `xiaohongshu`、`bilibili`。后两者在当前版本启动时明确报“暂未支持”，绝不按抖音逻辑处理。
- 输入可为单条或多条视频 URL、博主主页 URL，支持在本地配置的 `inputs.urls` 中直接填写，也支持 `inputs.urls_file` 指向每行一个 URL 的文本文件；两处链接合并去重。
- 主页任务尽量遍历全部公开视频。目标规模为单个主页约 100–1000 条视频，允许中断续跑。
- 每位博主生成一份 Markdown，包含每条视频的标题、原有 `#话题` 标签、视频链接、转写全文，以及已采集一级评论中点赞最高的 50 条。直接输入的视频按博主归档。
- 使用本机可见浏览器；需要登录或验证时由用户手动完成。数据不可访问时记录缺口，不绕过验证或权限限制。
- 使用小米 MiMo `mimo-v2.5-asr` 云端转写。完整流程默认不长期保存原视频或临时音频。
- 不传参数运行 `python main.py` 即执行完整流程。仓库提供脱敏的 `config.yaml`；本机复制为忽略上传的 `config.local.yaml` 并填写实际值。每个配置项也有对应命令行覆盖方式。

## 2. 命令与配置约定

| 命令 | 输入 | 输出 | 用途 |
| --- | --- | --- | --- |
| `python main.py` 或 `python main.py run` | 配置中的 URL | 报告、状态数据库 | 发现视频 → 采集 → 下载临时媒体 → 转写 → 报告 |
| `python main.py collect --url URL` | 视频或主页 URL | 状态数据库中的视频、标签和评论 | 单独运行采集模块 |
| `python main.py download --url URL` | 单条视频 URL | `download.output_dir` 中的媒体文件 | 单独运行下载模块；不要求 MiMo 密钥 |
| `python main.py transcribe --input FILE` | 本地音频或视频文件 | `asr.output_dir` 中同名 `.txt` 文件 | 单独运行转写模块；不要求抖音 URL 或浏览器登录 |
| `python main.py report` | 状态数据库 | `report.output_dir` 中的 Markdown | 根据现有数据重新生成报告，不再次采集或调用 MiMo |

- `--config PATH` 可选；默认优先读取程序目录中的 `config.local.yaml`，不存在时读取脱敏的 `config.yaml`。相对路径统一相对所选配置文件所在目录解析。
- 命令行优先级高于配置文件。每个配置叶子项提供对应的长参数，名称和注释以 `config.yaml` 为准；布尔值同时提供开启和关闭参数。`--url` 可重复，覆盖 `inputs.urls`；`--urls-file` 覆盖 `inputs.urls_file`。两种输入来源最终合并去重。
- `--url`、`--input` 是相应子命令的临时输入。校验按命令进行：`download` 不检查 MiMo 密钥，`transcribe` 不检查抖音 URL，`report` 不要求网络连接。
- 配置中的密钥可由 `--mimo-api-key` 临时覆盖。上传的 `config.yaml` 和 `config.example.yaml` 均不含密钥、个人链接或本机路径；实际使用的 `config.local.yaml` 由 `.gitignore` 排除。
- 只允许一次运行选择一个平台。抖音任务只接受抖音域名和可解析的抖音短链接；混入别的平台链接时给出逐条错误。

## 3. 模块边界与数据流

采用 Python 3.11+，将 CLI、配置、平台适配、采集、下载、转写、持久化和报告分开。模块提供可导入的函数或类，除 CLI/编排层外不直接读取 `config.yaml`、不依赖全局状态。

1. **配置与 CLI**：加载 YAML、解析命令行、做类型与范围校验，再把明确的配置对象传给各模块。`main.py` 只负责入口和子命令分发。
2. **平台适配器**：定义发现主页视频、读取视频元数据、遍历一级评论、解析媒体来源的统一接口；首版实现 `DouyinAdapter`。未来的小红书和 B站通过新适配器接入，不改下载、转写和报告模块。
3. **下载模块**：接收已解析的媒体来源和目标路径，保存媒体文件并返回文件路径、媒体类型及失败原因。独立 `download` 命令先通过平台适配器解析视频 URL，再调用下载模块；下载模块本身不处理评论或转写。
4. **转写模块**：接收本地音频或视频文件，使用 FFmpeg 转 MP3，并按 `asr.chunk_seconds` 分段；检查 Base64 后大小不超过 MiMo 限制，超限时继续细分。相邻片段按 `asr.overlap_seconds` 重叠并在合并文字时去重。调用 MiMo 后返回全文、分段状态和错误。该模块不调用采集或下载模块。
5. **存储模块**：SQLite 按“平台 + 视频 ID”保存元数据、一级评论、转写片段、处理状态与错误。各步骤成功后立即提交，保证中断续跑；`refresh_completed` 决定是否重新处理已完成视频。
6. **报告模块**：只读取标准化记录，不访问浏览器或 MiMo。每位博主输出一份 Markdown；无法确定作者的视频单独生成以视频 ID 命名的报告。报告采用原始可见话题标签，不生成 AI 标签。
7. **完整流程编排**：按发现 → 元数据与评论 → 临时下载 → 转写 → 报告的顺序调用独立模块。单条视频失败不终止整批；记录阶段和原因后继续。优雅中断时保留已完成结果，`report` 命令随时可生成现有数据的报告。

## 4. 采集与报告的准确性规则

- 只采集公开且当前浏览器会话实际可访问的内容。需要人工操作时暂停等待；超时、拒绝访问、媒体不可得等情况如实记录。不会自动破解验证码或绕过权限。
- 主页翻页到页面明确显示结束时，才标记视频列表完整。连续滚动没有新内容、达到配置上限、访问被限制时均标记为未确认完整。
- 评论仅计一级评论。尽量翻页直到明确到底；按数值化点赞数降序排列，点赞相同按稳定评论 ID 排序。只有确认读取全部一级评论时，报告才称“点赞最高的 50 条”；其他情况写“已采集评论的点赞前 50 条”，并列出采集数量及停止原因。
- 标签取页面显示的原有 `#话题`；没有话题和无法读取话题需分别标记。转写不可得时保留视频条目，并说明原因（除非用户关闭 `report.write_partial_results`）。
- 报告标题包含平台、博主、生成时间与采集覆盖情况；每条视频列出标题、标签、链接、转写状态与正文、评论点赞数和正文。Markdown 特殊字符需转义，避免评论破坏报告格式。

## 5. 实施顺序与验证

1. 建立项目结构、依赖声明、配置加载与 CLI；逐项验证 `config.yaml` 参数都有对应命令行覆盖，且无参数运行能按配置启动。
2. 实现标准化数据类型和 SQLite 状态存储，再实现抖音适配器的视频发现、元数据、一级评论和媒体来源解析。
3. 实现独立下载与转写模块，接入 MiMo；补齐 `collect`、`download`、`transcribe`、`report` 子命令和完整 `run` 编排。
4. 验证：配置覆盖与路径解析；URL 合并去重；主页分页与停止状态；评论点赞数换算、排序和不足 50 条；下载失败；音频超限分段；MiMo 超时重试；断点续跑；报告缺失标记。
5. 用实际可访问的抖音视频 URL 和主页 URL 做端到端试跑，检查标题、标签、评论、转写和报告。没有用户提供的测试链接时，用可访问的公开样例做冒烟测试，并记录因登录或页面限制无法验证的环节。

## 6. 外部约束

- 抖音开放平台的完整账号及评论数据能力有授权前提；任意公开博主的全部视频或全部评论无法保证从公开页面取得。参考：[抖音开放平台用户数据能力](https://developer.open-douyin.com/docs/resource/zh-CN/mini-app/open-capacity/basic-capacities/douyin)、[抖音用户服务协议](https://www.douyin.com/agreements/?id=6773906068725565448&ug_source=sem_baidu)。
- MiMo ASR 当前接收 MP3/WAV，Base64 编码音频大小上限为 10 MB；按官方接口调用并对长视频分段。参考：[小米 MiMo 语音识别文档](https://mimo.mi.com/docs/zh-CN/quick-start/usage-guide/audio/Speech-Recognition)。
- 本目录已有首版代码、依赖声明和离线测试；公开页面的端到端采集效果仍需按实际链接与登录状态验证。图文图片下载不在当前版本范围内。
